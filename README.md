# Utilities

GitHub Pagesで公開できる静的ユーティリティ集です。

## Tools

- `grid-cropper/` - 白い区切り線や余白を手がかりに、画像内の矩形領域をPNGとして切り抜くツールです。

## Local Test Images

検証用画像は `local-test/grid-cropper/input/` に置きます。このフォルダーは `.gitignore` で除外しています。
